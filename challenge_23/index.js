
const from = 'BAL'
const to   = 'LIB'
/* 
si from[i] esta en arreglo 

y from[i].index === to[i].index 

    tendria que guardar form[i] en un obj = from[i]+1
    
    si (obj.from[i] > 1)
    false


*/

function canReconfigure(from, to) {
    if(
        from.length !== to.length
        ){
        return false
    }

     
   }
   


